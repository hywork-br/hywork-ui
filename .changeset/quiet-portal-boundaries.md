---
"@hywork/ui": minor
---

Allow AdminShell to explicitly retain portal density in desktop and portaled
mobile navigation. Keep long FocusMode titles, body text and filenames inside
narrow viewports. Compose FileUpload progress from the shared tokenized Progress
contract instead of the browser-native visual. No transport or domain behavior.
